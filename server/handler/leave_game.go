package handler

import (
	"cli-typeracer/server/communication"
)

func LeaveGame(message *communication.Message) (communication.Message, error) {
	// player, err := state.LeaveGame(message.PlayerId)

	return communication.Message{}, nil
}
